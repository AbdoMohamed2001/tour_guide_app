import 'package:flutter/material.dart';

class AllTourCompanies extends StatelessWidget {
  const AllTourCompanies({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
